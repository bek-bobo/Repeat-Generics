spring:
  datasource:
      url: jdbc:postgresql://localhost:5432/test_db
      password: postgres
      username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true